{
  "name": "kizano-story-server",
  "version": "1.0.0",
  "description": "The server side API front that handles all the requests that could be made to story.markizano.net.",
  "main": "src/index.ts",
  "scripts": {
    "build": "tsc",
    "dist": "tsc",
    "start": "ts-node src/index.ts",
    "test": "echo \"Error: no test specified\" && exit 1",
    "clean": "rm -rf dist"
  },
  "keywords": [],
  "author": "Markizano Draconus <markizano@markizano.net>",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^20.12.7",
    "typescript": "^5.4.5"
  }
}
