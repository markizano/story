# KizanoStoryServer

This project is written in ExpressJS and is compiled from TypeScript.
This is the server side components to the story app and will serve as the API front to the DB.

Authentication and session management will also be handled by the server.
If there are components that can be rendered by a CDN agent of sorts, it will be configurable via the application.
